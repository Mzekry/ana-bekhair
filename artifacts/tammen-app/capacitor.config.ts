import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.lifekit.im_ok",
  appName: "Ana Bekhair",
  // Static web export produced by `npm run build` (expo export --platform web).
  webDir: "dist",
};

export default config;
